import { ref } from 'vue';

/**
 * Hằng số kiểm soát hiển thị component message không
 */
export const showMessage = ref(false);
/**
 * Hằng số kiểm soát hiển thị modal
 */
export const showModal = ref(false);
